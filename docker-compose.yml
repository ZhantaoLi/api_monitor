services:
  api-monitor:
    build: .
    container_name: api-monitor
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      - LOG_CLEANUP_ENABLED=1
      - LOG_MAX_SIZE_MB=512
    volumes:
      - ./data:/app/data
